package ex4student;

public class EmailEvaluator implements Evaluator {
    /** Returns true, if the param s contains a valid email address. */
    @Override
    public boolean isValid(String s) {
        //TODO
        return false;
    }

    /**
     * Returns true, if the param s is not an acceptable word.
     * That is, s is empty, or not all letters
     * are in 'A'...'Z', 'a'...'z' or '0'...'9'.
     */
    public boolean isNotWord(String s) {
        //TODO
        return false;
    }
}
